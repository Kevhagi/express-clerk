const path = require('path');

// Get the project root directory
const projectRoot = path.resolve(__dirname);

module.exports = {
  config: path.join(projectRoot, 'config', 'config.js'),
  'models-path': path.join(projectRoot, 'models'),
//   'seeders-path': path.join(projectRoot, 'seeders'),
  'migrations-path': path.join(projectRoot, 'migrations')
};